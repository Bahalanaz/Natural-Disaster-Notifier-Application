name: undergrad_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.0.0
  firebase_storage: ^13.2.0
  cupertino_icons: ^1.0.8
  get: ^4.7.3
  google_fonts: ^6.2.0
  geolocator: ^11.0.0
  shared_preferences: ^2.1.1
  intl: ^0.18.1
  permission_handler: ^11.0.0
  geocoding: ^2.1.0
  flutter_background_service: ^5.1.0
  flutter_local_notifications: ^20.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/emergency_hotlines.json